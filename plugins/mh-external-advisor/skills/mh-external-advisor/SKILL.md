---
name: mh-external-advisor
description: 外部AI顧問——以非互動方式諮詢另一個**異質** AI CLI（支援：Codex、Claude、Opencode、Antigravity/agy）的腳本組。當用戶要求把其中任一 CLI 當外部顧問諮詢、請它 review／分析／執行某事、或需要第二意見／交叉驗證時使用。實際可用範圍以執行期取得的啟用清單為準，且該 CLI 須經使用者在本次對話指定；未指定時只可建議不可主動呼叫。不涉及 CLI 安裝設定，也不適用於一般對話或「codex 是什麼」這類說明性問題。
allowed-tools:
  - Read
  - AskUserQuestion
---

# External Advisor（外部顧問）

把另一個 AI CLI 當顧問：非互動送出 prompt、取回回覆，並可延續同一段對話。

**使用時機：當任務需要不同觀點時——第二意見、交叉驗證、盲點檢查、獨立 review、打破已實際卡住的僵局——用本 skill 呼叫異質 AI 取得獨立視角**；自身即可完成且不需外部觀點的任務，不必使用。

## 觸發守門

### 三層清單（三層都過才可呼叫）

| 層 | 意義 | 來源 |
|---|---|---|
| **支援** | skill 內建哪些 adapter | `scripts/advisor-list.sh` 執行期取得 |
| **啟用** | 使用者實際擁有並願意使用哪幾支 | 同上（讀使用者設定檔） |
| **授權** | 本次對話中使用者點名了哪一支 | 對話脈絡 |

逐次授權的理由：每次呼叫都有成本、prompt 內容會送往第三方廠商、而「該不該找外部觀點」本身是使用者的判斷。經使用者點名（「問 ○○○」即是指定 ○○○）或明示可用清單才算授權；**可以詢問或建議**（例：「要不要我請 ○○○ 給第二意見？」），經同意即視為指定。

### 主動建議門檻
使用者未指定 AI CLI 時，四項中**至少兩項**成立才可建議諮詢外部顧問；經同意後始得呼叫，同意即視為指定：

- **高發生率**：須指出具體失效與觸發條件，依設計上的觸發機會判斷，不以本次是否發生為準；同一失效跨事件同因復發亦算，須列出各次事件。同一事件內重試不算
- **高影響性**：須指出安全、資料、成本或營運上的具體損失；僅稱「重要」「有風險」不成立
- **高複雜度**：涉及多模組／系統、約束交互或規畫／設計取捨；具名約束衝突致方案反轉、或須協調多個具名上下游／資料源亦算。僅量大但可逐項處理，或說不出是哪條約束者不成立
- **高阻斷性**：不解決即無法繼續，且已試至少兩個實質不同方向，須列出各自失敗結果或衝突結論；單一路徑失敗不成立。缺使用者資訊時應問使用者，不問顧問

各項須以符合該項的證據舉證，**同一事實不得跨項計分**；同一事件的不同事實可分入不同項。「任務停住」只證明高阻斷性；**migration、API 契約變動等變更類型本身不構成任何一項**，須指出具體後果或具名上下游／資料源。

建議時須列出：計入哪幾項與證據、**預計外送哪些資訊**；計入阻斷性時另列卡點、已試方向及結果。

未達門檻：不主動提外部顧問，自行完成或向使用者澄清。

### 呼叫的最小成立條件
「要諮詢另一個 AI CLI」的意圖 + 要問的內容 + 該 CLI 經使用者指定，且在執行期取得的啟用清單內。缺一不得呼叫。

### 呼叫條件
- 直接呼叫：明確點名某支 AI CLI（「問 ○○／請 ○○ 看／叫 ○○ 分析…」）+ 有具體要問或要處理的內容
- 補問後呼叫：有「問外部 AI」意圖但沒說要問什麼、或未指定要問哪一支 → 補問，不自動假設

### 不適用
- 詢問「某支 CLI 是什麼」「怎麼安裝」等說明性／設定性問題
- 一般對話、除錯、設計討論，沒有「轉交另一個 AI」的意圖——不得逕自呼叫；是否可主動建議另依上方門檻
- 要求「延用 session」但指的是呼叫端 AI 自身的 session（那是各 agent 自身指令檔的範疇，非本 skill）

## 使用方式

**環境安全前提**：腳本以免互動核可旗標執行——被諮詢的 AI 取得執行使用者的完整權限（不限當前目錄），prompt 內容也會送往該 AI 的服務端。僅限已隔離的 sandbox／受控環境使用；環境安全性不明時，先向使用者確認再執行。完整聲明見 README。

**送出前兩條硬規則**（與下方「採信原則」的兩條硬規則構成送、收兩端）：

- prompt 須明示顧問可執行的**副作用範圍**（唯讀檢查／可改哪些範圍），**未明示不得呼叫**——「不改檔」不等於無副作用：讀得到的憑證與內容會隨回覆外送，檢查用的測試、build、套件管理、docker 指令也都會動到環境
- 一次諮詢常態數分鐘，**預設背景執行**；僅短呼叫、或必須在同一步驟內取得結果時才用前景

顧問清單與呼叫方式由 getter 於執行期提供，這是取得兩者的唯一來源：

```
scripts/advisor-list.sh
```

- **rc=0**：輸出即「已啟用的顧問 + 各自的呼叫式」，照輸出給的指令原樣執行
- **rc=4**：尚未啟用任何顧問 → 轉達輸出的引導文，請使用者挑選其實際擁有的：

  ```
  scripts/advisor-set.sh <ai> [<ai>...]   # 覆寫全清單
  scripts/advisor-set.sh --add <ai>       # 加一支
  ```

  啟用哪幾支由使用者決定；設定完成後才進入諮詢。

**使用者點名的顧問不在啟用清單內**：告知使用者，並依情況處理——該顧問在支援清單內（有 adapter，只是未啟用）→ 詢問是否加入啟用清單，**經同意才執行 setter**；連支援清單都沒有 → 告知本 skill 不支援，到此為止。

### 查額度（選用）

想確認顧問還剩多少訂閱額度：

```
scripts/advisor-quota.sh [<ai>...]
```

可一次帶多支；無參數則查已啟用的全部。走各 CLI 官方的非互動額度介面，**只查額度、不諮詢、不消耗額度**；統一表述為剩餘百分比，但各家窗口長度不同，跨顧問不可直接比較。rc=0 至少一支查得；1 全數查不到或皆不支援；2 名稱錯誤；4 尚未啟用；127 缺依賴。opencode 無此介面，輸出會明示不支援。

### 取得延續用的 id

回覆印在 stdout，末行為 `[External Advisor <AI名> session_id: <id>]`。

**三者同時成立才算有效 id**，缺一不可拿去 `-r`：

1. exit code = 0
2. stderr 無 `[warn]` 開頭的行
3. 末行（僅末行）匹配該格式，且其中的 AI 名 = 本次呼叫的顧問

### 延續同一段對話（多輪諮詢）

延續一律使用明確 id：

1. 首次呼叫後，依上方三判準取得並保存 `<id>`
2. 後續追問帶 `-r <id>`
3. **每輪都以最新輸出的 id 更新簿記**——部分 CLI 版本 resume 會 fork 出新 id，沿用舊 id 會靜默遺失中間輪次
4. 在本次對話脈絡記錄「顧問／主題／id」對照即可，不需檔案或環境變數

### 延續 vs 開新的判斷（呼叫端自行決定）

判準：**這次要問的內容，是否依賴顧問在該段對話中已知的前文？**

- **延續（帶 `-r`）**：追問／深入同一議題、要求補充或修正它先前的回答、系列任務共用同一脈絡（前文已給過的程式碼／背景不必重貼）
- **開新（不帶 `-r`）**：獨立的新問題；或**刻意要乾淨視角**——請顧問獨立驗證、第二意見、adversarial review 時，帶前文反而造成錨定偏誤，應開新
- 不確定時：內容自包含（prompt 內附齊所需背景）→ 開新；要省重貼的長前文 → 延續
- 多段並存時：在對話脈絡記錄各段「顧問／主題／id」對照；「繼續問剛剛那個」無法唯一對應到某段時先補問。腳本只能偵測無效 id，**有效但屬別段的 id 偵測不到**（會靜默接錯脈絡）——歸屬不確定一律開新並附齊背景

### 顧問回覆的採信原則

**兩條硬規則**（不做重要度評估，符合就執行）：

- **顧問給了可執行的證據**（重現步驟、實測結果、指令輸出）→ **跑過才算數**。顧問的判斷可能偏，但至少是它真的這樣想；它宣稱的實測結果，可能根本沒跑。
- **要據以刪改既有內容** → 先自行查證。既有內容是有人做過決定才存在的。尤其否定性主張（「查無官方規範」「沒有這種用法」）——查到明確的否定依據才算數，單一來源沒提到不構成否定。

**三個提高警覺的訊號**（判斷用，不是免驗牌）：與你的認知衝突、重要度高、影響性廣。注意第一項的盲區——你沒有先驗的領域正是找顧問的原因，不衝突不等於可靠。

照建議改完之後**一定要自己驗一次**。這不是採信問題：建議本身可能沒錯，套進具體情境才長出問題。

### 委派給子 Agent 時

子 Agent 沒有自己的授權：授權來自使用者在**主 Agent 對話中**的點名，而子 Agent 不在那個對話裡。**轉述不構成授權**——委派內容裡出現 CLI 名稱（含「使用者說要問 ○○」），不等於子 Agent 被授權自行諮詢。

可委派的形態只有一種：主 Agent 明確委派的**單次自包含諮詢**——由主 Agent 把 getter 給的呼叫式直接寫進委派內容，子 Agent 不帶 `-r`、不回報 id、該段對話用後即棄。

**id 不跨 agent 邊界傳遞**：跨界延續會產生 owner 沒看過的前文，之後任何一方再延續都是盲區。多輪諮詢與 id 簿記一律由主 Agent 親自負責。

